# 2026-03-20 — GH: Favicon + Forgot Password

## Změny
- **Favicon**: Nový zlatý meč SVG (`public/favicon.svg`), opravena reference v index.html (`/vite.svg` → `/favicon.svg`)
- **Forgot password**: Nová stránka `/zapomenute-heslo` (ForgotPasswordPage.vue)
  - Email → 6-digit kód → nové heslo
  - Backend endpointy: `/api/auth/forgot-password` + `/api/auth/reset-password` (patched on server)
  - authApi: `forgotPassword()` + `resetPassword()` funkce
  - Link z LoginPage: "Zapomenuté heslo?"
- **Deploy**: Obojí (favicon + forgot password) nasazeno na gloomhaven.ongy.cz
